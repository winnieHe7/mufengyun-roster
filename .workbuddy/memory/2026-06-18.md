# 2026-06-18 工作日志

## 花名册网站开发（牟氏师门学生花名册系统）

### 完成内容
- 读取需求文件（功能及架构整理.docx + 3个样式PDF + 参考 urbancomp.net/links）
- 使用 Vite + React + Tailwind CSS 技术栈构建单页面应用
- 工程师因网络问题中断，主理人补全了剩余7个核心文件

### 文件清单
- `package.json` / `vite.config.js` / `tailwind.config.js` / `postcss.config.js` / `index.html`
- `src/main.jsx` - 入口
- `src/App.jsx` - 路由配置
- `src/index.css` - 全局样式 + Tailwind
- `src/data/mockStudents.js` - 44条模拟学生数据
- `src/context/AuthContext.jsx` - 认证上下文（localStorage模拟）
- `src/components/Header.jsx` - 顶部导航栏
- `src/components/StatCards.jsx` - 统计指标卡片
- `src/components/FilterBar.jsx` - 多维度筛选栏
- `src/components/StudentCard.jsx` - 学生卡片
- `src/components/StudentList.jsx` - 学生列表（按届分组，卡片/表格切换）
- `src/components/StudentDetail.jsx` - 学生详情Modal（键盘切换）
- `src/components/LoginForm.jsx` - 登录表单
- `src/components/RegisterForm.jsx` - 注册表单
- `src/components/ProfilePage.jsx` - 个人中心（编辑、隐私设置、完善度）
- `src/pages/HomePage.jsx` - 首页
- `src/pages/StatsPage.jsx` - 统计分析页（柱状图/饼图/SVG实现）
- `src/pages/AdminPage.jsx` - 管理后台（花名册管理 + 系统配置）
- `public/favicon.svg` - 网站图标

### 功能特性
- 多维度组合筛选（毕业年份/城市/行业/在校状态）
- 实时搜索（300ms防抖）
- 卡片/表格双视图切换
- 学生详情Modal（键盘左右切换）
- 模拟认证（localStorage，管理员 admin/admin123）
- CSV导出
- 统计分析（按届别/行业/城市柱状图 + 状态饼图）
- 个人中心（信息编辑/隐私设置/完善度进度条）
- 响应式设计（移动端适配）

### 运行方式
```bash
cd "H:\花名册开发"
npm install
npm run dev  # 开发服务器 http://localhost:5173
npm run build  # 生产构建
```

### 构建验证
- `npx vite build` 成功，零错误，输出 dist/ 目录
- 开发服务器正常响应

## 第二轮迭代（同日）

### 变更内容
1. **配色方案改为「墨韵学术」**：靛蓝(#1e3a5f)主色 + 暖灰(#f8f7f4)底 + 金色(#c9a96e)点缀
   - 更新 tailwind.config.js：新增 primary/accent/warm 三套色系
   - 更新 index.css：渐变、滚动条、表格、Hero背景纹理、章节标题装饰
   - 批量替换组件中硬编码的 #1a73e8 → #1e3a5f

2. **去掉注册功能，改为管理员分发账号**
   - 删除 RegisterForm.jsx
   - AuthContext：移除 register()，新增 createAccount() / deleteAccount() / resetPassword()
   - App.jsx：移除 /register 路由
   - Header.jsx：移除注册按钮，新增「关于我们」导航
   - LoginForm.jsx：去掉注册入口，提示"账号由管理员统一分发"
   - AdminPage.jsx：新增「账号分发」Tab，管理员可创建/删除/重置密码

3. **新增导师简介 + 团队简介模块**
   - 新建 src/data/siteInfo.js：导师信息（研究方向/教育/经历/成果）+ 团队信息（简介/方向/统计/亮点）
   - 新建 src/components/HeroSection.jsx：首页顶部精简版双卡片（导师卡+团队卡），靛蓝渐变背景
   - 新建 src/pages/AboutPage.jsx：独立完整版页面（导师完整简历 + 团队研究方向/亮点）
   - HomePage.jsx：在统计卡片上方插入 HeroSection
   - App.jsx：新增 /about 路由

### 文件变更清单
- 修改：tailwind.config.js, index.css, App.jsx, Header.jsx, AuthContext.jsx, LoginForm.jsx, AdminPage.jsx, HomePage.jsx, StudentCard.jsx, StudentDetail.jsx, ProfilePage.jsx, StatsPage.jsx
- 新增：src/data/siteInfo.js, src/components/HeroSection.jsx, src/pages/AboutPage.jsx
- 删除：src/components/RegisterForm.jsx

### 构建验证
- 第二轮 `npx vite build` 成功，零错误（254KB JS + 25KB CSS）

## 第三轮迭代（同日）

### 变更内容
1. **网站名称统一改为「牟凤云团队花名册」**：牟老师→牟凤云，牟氏师门→牟凤云团队（全站替换，含 index.html title、Header、Footer、siteInfo.js、AdminPage 公告等）

2. **分组方式从「xx届毕业生」改为「xx级研究生」**：
   - mockStudents.js：新增 degree 字段（默认"研究生"），通过 export map 注入
   - StudentList.jsx：分组逻辑从 graduateYear 改为 enrollYear + degree，标题显示"20XX级研究生"
   - FilterBar.jsx：新增「学历层次」筛选下拉（研究生/本科生/博士生）
   - HomePage.jsx：筛选条件增加 degree，提取 degrees 选项列表，搜索支持 enrollYear
   - StudentDetail.jsx：详情页增加「学历层次」字段，「入学年份」→「入学年级」
   - AdminPage.jsx：管理后台表格「届别」→「年级」+「学历」列，创建账号表单增加学历选择
   - StatsPage.jsx：统计页「按毕业届别」→「按入学年级」，新增「按学历层次分布」图表
   - AuthContext.jsx：createAccount 新增 degree 字段

### 构建验证
- 第三轮 `npx vite build` 成功，零错误（257KB JS + 25KB CSS）
